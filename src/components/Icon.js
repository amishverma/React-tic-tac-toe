import React from "react";
import { FaTimes, FaPen, FaRegCirlce } from "react-icons/fa";

function Icon({ name }) {
  return (
    <h1>
      <FaTimes className="icon" />
    </h1>
  );
}

export default Icon;
